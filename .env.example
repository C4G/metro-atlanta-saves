# Database Configuration
DATABASE_HOST=localhost
DATABASE_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_password_here
POSTGRES_DB=metro_atlanta_saves
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${DATABASE_HOST}:${DATABASE_PORT}/${POSTGRES_DB}?schema=public

# API Configuration
API_PORT=3000
FE_PORT=4200
# Comma-free single origin the API accepts browser requests from. In production
# the frontend and API share one origin, so this only matters for local tooling.
CORS_ORIGIN=http://localhost:4200

# Where user uploads are written. Defaults to <cwd>/.uploads when unset; in
# production this is a Docker volume.
UPLOAD_DIR=

# JWT Configuration
JWT_SECRET=your_jwt_secret_here

# Mail Configuration
MAIL_HOST=smtp.example.com
MAIL_USER=your_mail_user
MAIL_PORT=587
MAIL_PASSWORD=your_mail_password
MAIL_FROM=noreply@example.com
# =========================
# Web Push (VAPID) Configuration
# =========================
VAPID_PUBLIC_KEY=your_vapid_public_key_here
VAPID_PRIVATE_KEY=your_vapid_private_key_here
VAPID_SUBJECT=mailto:admin@yourdomain.com
