# backend-users-on-programs

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build backend-users-on-programs` to build the library.

## Running unit tests

Run `nx test backend-users-on-programs` to execute the unit tests via [Jest](https://jestjs.io).
