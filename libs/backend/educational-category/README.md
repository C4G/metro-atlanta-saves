# backend-educational-category

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build backend-educational-category` to build the library.

## Running unit tests

Run `nx test backend-educational-category` to execute the unit tests via [Jest](https://jestjs.io).
