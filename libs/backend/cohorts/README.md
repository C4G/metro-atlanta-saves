# backend-cohorts

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build backend-cohorts` to build the library.

## Running unit tests

Run `nx test backend-cohorts` to execute the unit tests via [Jest](https://jestjs.io).
