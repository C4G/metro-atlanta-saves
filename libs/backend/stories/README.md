# backend-stories

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build backend-stories` to build the library.

## Running unit tests

Run `nx test backend-stories` to execute the unit tests via [Jest](https://jestjs.io).
