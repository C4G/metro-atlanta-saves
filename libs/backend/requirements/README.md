# backend-requirements

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build backend-requirements` to build the library.

## Running unit tests

Run `nx test backend-requirements` to execute the unit tests via [Jest](https://jestjs.io).
