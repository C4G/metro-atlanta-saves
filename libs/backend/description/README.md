# backend-description

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build backend-description` to build the library.

## Running unit tests

Run `nx test backend-description` to execute the unit tests via [Jest](https://jestjs.io).
