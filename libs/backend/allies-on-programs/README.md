# backend-allies-on-programs

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build backend-allies-on-programs` to build the library.

## Running unit tests

Run `nx test backend-allies-on-programs` to execute the unit tests via [Jest](https://jestjs.io).
