# @mas/backend-blogs

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build @mas/backend-blogs` to build the library.

## Running unit tests

Run `nx test @mas/backend-blogs` to execute the unit tests via [Jest](https://jestjs.io).
