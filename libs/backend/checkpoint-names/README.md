# @mas/backend-checkpoint-names

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build @mas/backend-checkpoint-names` to build the library.

## Running unit tests

Run `nx test @mas/backend-checkpoint-names` to execute the unit tests via [Jest](https://jestjs.io).
