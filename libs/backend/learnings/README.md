# backend-learnings

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build backend-learnings` to build the library.

## Running unit tests

Run `nx test backend-learnings` to execute the unit tests via [Jest](https://jestjs.io).
