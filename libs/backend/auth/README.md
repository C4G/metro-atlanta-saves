# @mas/backend-auth

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build @mas/backend-auth` to build the library.

## Running unit tests

Run `nx test @mas/backend-auth` to execute the unit tests via [Jest](https://jestjs.io).
