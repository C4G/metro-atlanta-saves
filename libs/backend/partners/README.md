# backend-partners

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build backend-partners` to build the library.

## Running unit tests

Run `nx test backend-partners` to execute the unit tests via [Jest](https://jestjs.io).
