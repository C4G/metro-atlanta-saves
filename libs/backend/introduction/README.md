# backend-introduction

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build backend-introduction` to build the library.

## Running unit tests

Run `nx test backend-introduction` to execute the unit tests via [Jest](https://jestjs.io).
